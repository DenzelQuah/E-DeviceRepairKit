class Shop {
  final String id;
  final String name;
  final double distance;
  // You can add more fields later (e.g., address, rating)

  Shop({
    required this.id,
    required this.name,
    required this.distance,
  });
}